---
# SSL Certificate Discovery and Setup
# Purpose: Find SSL certificates from ssl/ directory or generate self-signed
# Priority:
#   1. Files on disk in ssl/ directory (exact domain, then wildcard parent)
#   2. Vault fallback: materialize from secrets.ssl_certificates (only if no files on disk)
#   3. Self-signed fallback: Generate certificates
#
# Note: For each candidate, we verify that the certificate and key modulus match
# before using them. This prevents deployment failures from mismatched cert/key pairs.
# Files on disk always take priority over vault — vault materialization never overwrites
# existing certificate files.

- name: Determine SSL source directory
  set_fact:
    ssl_source_dir: "{{ ssl_certificates_path | default(playbook_dir + '/../../ssl') }}"

- name: Resolve SSL source directory to absolute path
  command: "realpath {{ ssl_source_dir }}"
  register: ssl_dir_resolved
  delegate_to: localhost
  become: false
  changed_when: false

- name: Set resolved SSL source directory
  set_fact:
    ssl_source_dir: "{{ ssl_dir_resolved.stdout }}"

- name: Ensure SSL source directory exists on controller
  file:
    path: "{{ ssl_source_dir }}"
    state: directory
    mode: "0755"
  delegate_to: localhost
  become: false

- name: Extract parent domain for wildcard lookup
  set_fact:
    parent_domain: "{{ full_domain.split('.', 1)[1] if full_domain.split('.')|length > 2 else full_domain }}"

- name: List SSL source directory contents
  shell: "ls -la {{ ssl_source_dir }}/ 2>&1 || echo 'DIRECTORY_EMPTY_OR_MISSING'"
  register: ssl_dir_listing
  delegate_to: localhost
  become: false
  changed_when: false

- name: Debug SSL lookup
  debug:
    msg: |
      SSL Certificate Lookup:
      - Full domain: {{ full_domain }}
      - Parent domain: {{ parent_domain }}
      - Source directory: {{ ssl_source_dir }}
      - Directory contents:
      {{ ssl_dir_listing.stdout }}

# Check file existence using shell (stat module has issues with delegated
# tasks on some Proxmox setups — shell-based checks are reliable)
- name: Check for SSL certificate files on controller
  shell: |
    exact_fullchain=false; exact_cert=false; exact_key=false
    wildcard_fullchain=false; wildcard_cert=false; wildcard_key=false
    [ -f "{{ ssl_source_dir }}/{{ full_domain }}.fullchain.crt" ] && exact_fullchain=true
    [ -f "{{ ssl_source_dir }}/{{ full_domain }}.crt" ] && exact_cert=true
    [ -f "{{ ssl_source_dir }}/{{ full_domain }}.key" ] && exact_key=true
    [ -f "{{ ssl_source_dir }}/{{ parent_domain }}.fullchain.crt" ] && wildcard_fullchain=true
    [ -f "{{ ssl_source_dir }}/{{ parent_domain }}.crt" ] && wildcard_cert=true
    [ -f "{{ ssl_source_dir }}/{{ parent_domain }}.key" ] && wildcard_key=true
    echo "{\"exact_fullchain\":$exact_fullchain,\"exact_cert\":$exact_cert,\"exact_key\":$exact_key,\"wildcard_fullchain\":$wildcard_fullchain,\"wildcard_cert\":$wildcard_cert,\"wildcard_key\":$wildcard_key}"
  register: ssl_file_check
  delegate_to: localhost
  become: false
  changed_when: false

- name: Parse SSL file check results
  set_fact:
    ssl_check: "{{ ssl_file_check.stdout | from_json }}"

- name: Debug SSL file check results
  debug:
    msg: "SSL file check: {{ ssl_check }}"

# Build compatibility variables from shell check results
- name: Set exact fullchain stat compatibility
  set_fact:
    exact_fullchain_exists: "{{ ssl_check.exact_fullchain }}"
    exact_cert_exists: "{{ ssl_check.exact_cert }}"
    exact_key_exists: "{{ ssl_check.exact_key }}"
    wildcard_fullchain_exists: "{{ ssl_check.wildcard_fullchain }}"
    wildcard_cert_exists: "{{ ssl_check.wildcard_cert }}"
    wildcard_key_exists: "{{ ssl_check.wildcard_key }}"

# Determine if any cert files exist on disk already
- name: Check if any certificate files were found on disk
  set_fact:
    ssl_files_found_on_disk: >-
      {{
        (exact_fullchain_exists and exact_key_exists) or
        (exact_cert_exists and exact_key_exists) or
        (wildcard_fullchain_exists and wildcard_key_exists) or
        (wildcard_cert_exists and wildcard_key_exists)
      }}

# Vault fallback: only materialize from vault when no cert files exist on disk
- name: Materialize certificate from vault (no files found on disk)
  copy:
    content: "{{ secrets.ssl_certificates.certificate }}"
    dest: "{{ ssl_source_dir }}/{{ full_domain }}.crt"
    mode: "0644"
  delegate_to: localhost
  become: false
  when:
    - not ssl_files_found_on_disk
    - secrets is defined
    - secrets.ssl_certificates is defined
    - secrets.ssl_certificates.certificate is defined
    - (secrets.ssl_certificates.certificate | trim | length) > 0

- name: Materialize private key from vault (no files found on disk)
  copy:
    content: "{{ secrets.ssl_certificates.private_key }}"
    dest: "{{ ssl_source_dir }}/{{ full_domain }}.key"
    mode: "0600"
  delegate_to: localhost
  become: false
  when:
    - not ssl_files_found_on_disk
    - secrets is defined
    - secrets.ssl_certificates is defined
    - secrets.ssl_certificates.private_key is defined
    - (secrets.ssl_certificates.private_key | trim | length) > 0

- name: Materialize fullchain cert from vault (no files found on disk)
  copy:
    content: "{{ secrets.ssl_certificates.certificate }}\n{{ secrets.ssl_certificates.chain }}"
    dest: "{{ ssl_source_dir }}/{{ full_domain }}.fullchain.crt"
    mode: "0644"
  delegate_to: localhost
  become: false
  when:
    - not ssl_files_found_on_disk
    - secrets is defined
    - secrets.ssl_certificates is defined
    - secrets.ssl_certificates.certificate is defined
    - secrets.ssl_certificates.chain is defined
    - (secrets.ssl_certificates.certificate | trim | length) > 0
    - (secrets.ssl_certificates.chain | trim | length) > 0

# Re-check file existence after potential vault materialization
- name: Re-check SSL files after vault materialization
  shell: |
    exact_fullchain=false; exact_cert=false; exact_key=false
    [ -f "{{ ssl_source_dir }}/{{ full_domain }}.fullchain.crt" ] && exact_fullchain=true
    [ -f "{{ ssl_source_dir }}/{{ full_domain }}.crt" ] && exact_cert=true
    [ -f "{{ ssl_source_dir }}/{{ full_domain }}.key" ] && exact_key=true
    echo "{\"exact_fullchain\":$exact_fullchain,\"exact_cert\":$exact_cert,\"exact_key\":$exact_key}"
  register: ssl_recheck
  delegate_to: localhost
  become: false
  changed_when: false
  when: not ssl_files_found_on_disk

- name: Update file existence after vault materialization
  set_fact:
    exact_fullchain_exists: "{{ (ssl_recheck.stdout | from_json).exact_fullchain }}"
    exact_cert_exists: "{{ (ssl_recheck.stdout | from_json).exact_cert }}"
    exact_key_exists: "{{ (ssl_recheck.stdout | from_json).exact_key }}"
    ssl_files_found_on_disk: >-
      {{
        ((ssl_recheck.stdout | from_json).exact_fullchain and (ssl_recheck.stdout | from_json).exact_key) or
        ((ssl_recheck.stdout | from_json).exact_cert and (ssl_recheck.stdout | from_json).exact_key)
      }}
  when: not ssl_files_found_on_disk and ssl_recheck is defined and ssl_recheck.stdout is defined

# Verify cert/key pairs match (modulus comparison)
- name: Verify exact fullchain cert matches key
  shell: |
    cert_mod=$(openssl x509 -noout -modulus -in "{{ ssl_source_dir }}/{{ full_domain }}.fullchain.crt" 2>/dev/null | md5sum | cut -d' ' -f1)
    key_mod=$(openssl rsa -noout -modulus -in "{{ ssl_source_dir }}/{{ full_domain }}.key" 2>/dev/null | md5sum | cut -d' ' -f1)
    [ "$cert_mod" = "$key_mod" ] && echo "MATCH" || echo "MISMATCH"
  register: exact_fullchain_verify
  delegate_to: localhost
  become: false
  changed_when: false
  when:
    - exact_fullchain_exists | default(false)
    - exact_key_exists | default(false)

- name: Verify exact cert matches key
  shell: |
    cert_mod=$(openssl x509 -noout -modulus -in "{{ ssl_source_dir }}/{{ full_domain }}.crt" 2>/dev/null | md5sum | cut -d' ' -f1)
    key_mod=$(openssl rsa -noout -modulus -in "{{ ssl_source_dir }}/{{ full_domain }}.key" 2>/dev/null | md5sum | cut -d' ' -f1)
    [ "$cert_mod" = "$key_mod" ] && echo "MATCH" || echo "MISMATCH"
  register: exact_cert_verify
  delegate_to: localhost
  become: false
  changed_when: false
  when:
    - exact_cert_exists | default(false)
    - exact_key_exists | default(false)

- name: Verify wildcard fullchain cert matches key
  shell: |
    cert_mod=$(openssl x509 -noout -modulus -in "{{ ssl_source_dir }}/{{ parent_domain }}.fullchain.crt" 2>/dev/null | md5sum | cut -d' ' -f1)
    key_mod=$(openssl rsa -noout -modulus -in "{{ ssl_source_dir }}/{{ parent_domain }}.key" 2>/dev/null | md5sum | cut -d' ' -f1)
    [ "$cert_mod" = "$key_mod" ] && echo "MATCH" || echo "MISMATCH"
  register: wildcard_fullchain_verify
  delegate_to: localhost
  become: false
  changed_when: false
  when:
    - parent_domain != full_domain
    - wildcard_fullchain_exists | default(false)
    - wildcard_key_exists | default(false)

- name: Verify wildcard cert matches key
  shell: |
    cert_mod=$(openssl x509 -noout -modulus -in "{{ ssl_source_dir }}/{{ parent_domain }}.crt" 2>/dev/null | md5sum | cut -d' ' -f1)
    key_mod=$(openssl rsa -noout -modulus -in "{{ ssl_source_dir }}/{{ parent_domain }}.key" 2>/dev/null | md5sum | cut -d' ' -f1)
    [ "$cert_mod" = "$key_mod" ] && echo "MATCH" || echo "MISMATCH"
  register: wildcard_cert_verify
  delegate_to: localhost
  become: false
  changed_when: false
  when:
    - parent_domain != full_domain
    - wildcard_cert_exists | default(false)
    - wildcard_key_exists | default(false)

# Determine which certificate to use (with verification)
- name: Use exact domain certificate (fullchain) if verified
  set_fact:
    ssl_cert_source: "{{ ssl_source_dir }}/{{ full_domain }}.fullchain.crt"
    ssl_key_source: "{{ ssl_source_dir }}/{{ full_domain }}.key"
    ssl_mode_used: "exact-fullchain"
  when:
    - exact_fullchain_exists | default(false)
    - exact_key_exists | default(false)
    - exact_fullchain_verify.stdout | default('') == 'MATCH'

- name: Use exact domain certificate (without fullchain) if verified
  set_fact:
    ssl_cert_source: "{{ ssl_source_dir }}/{{ full_domain }}.crt"
    ssl_key_source: "{{ ssl_source_dir }}/{{ full_domain }}.key"
    ssl_mode_used: "exact"
  when:
    - ssl_mode_used is not defined
    - exact_cert_exists | default(false)
    - exact_key_exists | default(false)
    - exact_cert_verify.stdout | default('') == 'MATCH'

- name: Use wildcard parent certificate (fullchain) if verified
  set_fact:
    ssl_cert_source: "{{ ssl_source_dir }}/{{ parent_domain }}.fullchain.crt"
    ssl_key_source: "{{ ssl_source_dir }}/{{ parent_domain }}.key"
    ssl_mode_used: "wildcard-fullchain"
  when:
    - ssl_mode_used is not defined
    - parent_domain != full_domain
    - wildcard_fullchain_exists | default(false)
    - wildcard_key_exists | default(false)
    - wildcard_fullchain_verify.stdout | default('') == 'MATCH'

- name: Use wildcard parent certificate (without fullchain) if verified
  set_fact:
    ssl_cert_source: "{{ ssl_source_dir }}/{{ parent_domain }}.crt"
    ssl_key_source: "{{ ssl_source_dir }}/{{ parent_domain }}.key"
    ssl_mode_used: "wildcard"
  when:
    - ssl_mode_used is not defined
    - parent_domain != full_domain
    - wildcard_cert_exists | default(false)
    - wildcard_key_exists | default(false)
    - wildcard_cert_verify.stdout | default('') == 'MATCH'

- name: Generate self-signed certificate (fallback)
  set_fact:
    ssl_mode_used: "selfsigned"
  when: ssl_mode_used is not defined

- name: Warn if certificates found but not verified
  debug:
    msg: |
      ⚠️  WARNING: SSL certificates found but cert/key modulus mismatch detected!
      Keeping existing certificates on target. Please verify your SSL certificate and key files match.
      
      Checked files:
      {% if exact_fullchain_exists | default(false) and exact_key_exists | default(false) %}
      - {{ full_domain }}.fullchain.crt + .key: {{ exact_fullchain_verify.stdout | default('NOT CHECKED') }}
      {% endif %}
      {% if exact_cert_exists | default(false) and exact_key_exists | default(false) %}
      - {{ full_domain }}.crt + .key: {{ exact_cert_verify.stdout | default('NOT CHECKED') }}
      {% endif %}
      {% if parent_domain != full_domain and wildcard_fullchain_exists | default(false) and wildcard_key_exists | default(false) %}
      - {{ parent_domain }}.fullchain.crt + .key: {{ wildcard_fullchain_verify.stdout | default('NOT CHECKED') }}
      {% endif %}
      {% if parent_domain != full_domain and wildcard_cert_exists | default(false) and wildcard_key_exists | default(false) %}
      - {{ parent_domain }}.crt + .key: {{ wildcard_cert_verify.stdout | default('NOT CHECKED') }}
      {% endif %}
  when:
    - ssl_mode_used == 'selfsigned'
    - ssl_files_found_on_disk | default(false)

- name: Display SSL certificate decision
  debug:
    msg: |
      SSL Certificate Mode: {{ ssl_mode_used }}
      {% if ssl_mode_used != 'selfsigned' %}
      Certificate: {{ ssl_cert_source }}
      Key: {{ ssl_key_source }}
      {% else %}
      Self-signed certificate will be generated for {{ full_domain }}
      {% endif %}

# Copy certificates if we found them
- name: Create SSL directory on target
  file:
    path: /etc/ssl/busibox
    state: directory
    mode: "0755"
    owner: root
    group: root

- name: Copy SSL certificate to target
  copy:
    src: "{{ ssl_cert_source }}"
    dest: "/etc/ssl/busibox/{{ full_domain }}.crt"
    mode: "0644"
    owner: root
    group: root
  when: ssl_mode_used != 'selfsigned'

- name: Copy SSL key to target
  copy:
    src: "{{ ssl_key_source }}"
    dest: "/etc/ssl/busibox/{{ full_domain }}.key"
    mode: "0600"
    owner: root
    group: root
  when: ssl_mode_used != 'selfsigned'

# Self-signed fallback: NEVER overwrite existing certs on the target.
# Only generate self-signed if no cert files exist on the target at all.
- name: Check if cert already exists on target
  stat:
    path: "/etc/ssl/busibox/{{ full_domain }}.crt"
  register: target_cert_stat
  when: ssl_mode_used == 'selfsigned'

- name: Check if key already exists on target
  stat:
    path: "/etc/ssl/busibox/{{ full_domain }}.key"
  register: target_key_stat
  when: ssl_mode_used == 'selfsigned'

- name: Warn - keeping existing target certs (no source certs found)
  debug:
    msg: |
      ⚠️  No SSL source certificates found in {{ ssl_source_dir }}, but valid cert/key
      already exists on target at /etc/ssl/busibox/. Keeping existing certificates.
      To update certificates, place new cert files in the ssl/ directory.
  when:
    - ssl_mode_used == 'selfsigned'
    - (target_cert_stat.stat.exists | default(false)) and (target_key_stat.stat.exists | default(false))

- name: Generate self-signed certificate (no certs exist anywhere)
  command: >
    openssl req -x509 -nodes -days 365 -newkey rsa:2048
    -keyout /etc/ssl/busibox/{{ full_domain }}.key
    -out /etc/ssl/busibox/{{ full_domain }}.crt
    -subj "/C=US/ST=State/L=City/O=Organization/CN={{ full_domain }}"
  when:
    - ssl_mode_used == 'selfsigned'
    - not (target_cert_stat.stat.exists | default(false)) or not (target_key_stat.stat.exists | default(false))

# Set final paths for nginx templates
- name: Set SSL certificate paths for NGINX
  set_fact:
    ssl_certificate: "/etc/ssl/busibox/{{ full_domain }}.crt"
    ssl_certificate_key: "/etc/ssl/busibox/{{ full_domain }}.key"
